# 4.07 아이디/비밀번호 찾기 기능 추가 — 기능설계 및 구현 계획서

> 작성일: 2026-08-05
> 브랜치: `claude/id-pass-find-feature-bf99sk`
> 관련 문서: `plan.md`(전체 리뉴얼 계획), `lib/auth.ts`, `lib/auth.config.ts`

---

## 1. 개요

### 1.1 배경 및 목적

현재 `/login` 페이지는 이메일 + 비밀번호 로그인만 제공하며, 비밀번호를 잊었거나 가입 시 사용한 이메일이 기억나지 않는 회원을 위한 복구 수단이 없다. 회원 문의가 관리자에게 직접 들어오는 상황을 막기 위해 **아이디(이메일) 찾기**와 **비밀번호 찾기(재설정)** 기능을 셀프서비스로 제공한다.

### 1.2 현황 분석 (As-Is)

| 항목 | 현재 상태 |
|------|-----------|
| 로그인 방식 | `email` + `password` (Credentials Provider), `email`이 곧 로그인 아이디 |
| `User` 모델 | `id, email, name, password, role, isActive, emailVerified, image` — **휴대폰번호 필드 없음** |
| 본인확인 수단 | 없음 (SMS/이메일 발송 인프라 미구축) |
| 이메일 발송 | 미구현 — `nodemailer`, `resend` 등 관련 패키지 `package.json`에 없음 |
| 임시토큰 저장소 | `VerificationToken` 모델 존재하나 NextAuth Email Provider용으로 예약된 스키마, 현재 미사용 |
| Rate Limit | `lib/rate-limit.ts`에 `authRateLimit`(분당 10회) 존재 — 재사용 가능 |
| 비밀번호 해시 | `bcryptjs`, `hash(password, 12)` — 기존 회원가입 로직과 동일 방식 재사용 |

### 1.3 핵심 설계 이슈

이 서비스는 **이메일 = 로그인 아이디**이므로 "아이디 찾기"는 곧 "가입한 이메일 찾기"가 된다. 이메일을 모르는 사용자에게 이메일로 안내를 보낼 수 없으므로, 이메일이 아닌 **다른 본인확인 수단(이름 + 휴대폰번호)**이 반드시 필요하다. → `User` 모델에 `phone` 필드 신규 추가가 선행되어야 한다.

---

## 2. 기능 정의

### 2.1 아이디(이메일) 찾기 — `/find-id`

1. 사용자가 **이름 + 휴대폰번호**를 입력
2. 일치하는 회원이 있으면 등록된 이메일을 **마스킹 처리**하여 화면에 표시 (예: `pa****@gmail.com`)
3. 일치하는 회원이 없어도 "일치하는 회원 없음"과 "해당 정보로 가입된 계정 없음"을 **동일한 문구**로 응답 (계정 존재 여부 유추 방지)

### 2.2 비밀번호 찾기(재설정) — `/find-password`

1. 사용자가 **이메일(아이디)** 입력
2. 서버가 6자리 인증코드를 생성해 **등록된 이메일로 발송**, Redis에 TTL 5분으로 저장
3. 사용자가 인증코드 입력 → 서버 검증 (최대 5회 오입력 허용)
4. 인증 성공 시 1회용 재설정 토큰 발급 → **새 비밀번호 설정** 화면으로 이동
5. 새 비밀번호 저장 (`bcryptjs` 해시, 기존 회원가입 정책과 동일: 8자 이상 + 영문/숫자 포함)
6. 재설정 완료 후 기존 세션 전체 무효화(재로그인 유도) — `Session` 테이블에서 `userId` 매칭 레코드 삭제

### 2.3 보안 원칙

- **계정 존재 여부 비노출**: 존재하지 않는 이메일/전화번호로 요청해도 "성공" 응답과 동일한 UX 흐름 유지 (타이밍 공격/열거 공격 방지)
- **Rate Limiting**: 기존 `authRateLimit`과 별도로 `findAccountRateLimit`(IP 기준 분당 5회), 이메일당 인증코드 발송은 1분에 1회로 제한
- **인증코드**: 6자리 숫자, TTL 5분, 5회 오입력 시 코드 즉시 폐기 후 재요청 유도
- **재설정 토큰**: 1회용, TTL 15분, 사용 즉시 폐기(soft delete 또는 `usedAt` 기록)
- **이메일 마스킹**: 로컬파트 앞 2자만 노출 (`pa****@domain.com`)

---

## 3. 데이터 모델 변경 (Prisma)

### 3.1 `User` 모델 — `phone` 필드 추가

```prisma
model User {
  id            String    @id @default(cuid())
  email         String    @unique
  name          String?
  phone         String?   @unique   // 신규: 아이디 찾기 본인확인용
  password      String?
  role          UserRole  @default(MEMBER)
  isActive      Boolean   @default(true)
  emailVerified DateTime?
  image         String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  lastLoginAt   DateTime?

  donations Donation[]
  accounts  Account[]
  sessions  Session[]
  passwordResetTokens PasswordResetToken[]
}
```

> 회원가입(`/register`) 폼에도 `phone` 입력란 추가 필요 (선택 입력이 아니라 **필수 입력**으로 전환 권장 — 선택 입력일 경우 아이디 찾기 기능 자체를 이용 못 하는 회원이 발생하기 때문).

### 3.2 신규 모델 — `PasswordResetToken`

기존 `VerificationToken`은 NextAuth Email Provider 전용 스키마(복합키 구조)라 재설정 흐름의 상태(코드 검증 완료 여부, 사용 여부)를 담기 부적합하므로 별도 모델을 신설한다.

```prisma
model PasswordResetToken {
  id        String    @id @default(cuid())
  userId    String
  code      String    // 6자리 인증코드 (해시 저장 권장)
  token     String    @unique // 코드 검증 성공 후 발급되는 1회용 재설정 토큰
  attempts  Int       @default(0)
  verified  Boolean   @default(false)
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}
```

- `code`는 평문 저장하지 않고 `bcryptjs` 또는 HMAC으로 해시 보관 (DB 유출 시에도 코드 재사용 불가)
- Redis 대신 DB 테이블로 설계 — Upstash Redis는 이미 rate-limit 용도로 쓰고 있어 TTL 관리가 겹치지 않도록 상태값은 DB에 두고, **1분 재발송 제한**만 Redis(`ratelimit:reset-code`)로 별도 처리

### 3.3 마이그레이션 작업

| 작업 | 명령 |
|------|------|
| 스키마 수정 후 클라이언트 재생성 | `npm run db:generate` |
| DB 반영 (개발) | `npm run db:push` |
| 기존 회원 `phone` 값 백필 | 관리자 안내 또는 마이페이지에서 자진 등록 유도 (마이그레이션 시 `phone`은 `nullable`로 유지, 강제 NOT NULL 전환은 별도 후속 작업) |

---

## 4. 이메일 발송 인프라 (신규 구축 필요)

현재 이메일 발송 수단이 전혀 없으므로 인프라 선정이 선행되어야 한다.

| 후보 | 장점 | 비고 |
|------|------|------|
| **Resend** (권장) | Next.js/Vercel 생태계 표준, 무료 티어(월 3,000건) 충분, API 심플 | `resend` 패키지 추가, `RESEND_API_KEY` env 필요 |
| Nodemailer + SMTP | 범용적, 기존 이메일 계정(Gmail/Naver 등) 활용 가능 | 발신 도메인 신뢰도 낮음, 스팸함 처리 위험 |
| Supabase Auth Email | 이미 Supabase 연동 중 | NextAuth Credentials 방식과 이중화되어 설정 복잡도 증가 |

**권장안**: `Resend` 채택. `.env.example`에 아래 항목 추가.

```env
# Resend (이메일 발송)
RESEND_API_KEY="re_your-api-key"
RESEND_FROM_EMAIL="no-reply@paxchristi.or.kr"
```

이메일 템플릿(HTML) 파일은 `lib/email/templates/password-reset-code.tsx` 형태로 관리하고, 발송 유틸은 `lib/email.ts`에 `sendPasswordResetCode(to, code)` 함수로 구현한다.

---

## 5. 서버 로직 설계

### 5.1 신규 Zod 스키마 — `lib/validations/find-account.schema.ts`

```ts
export const findIdSchema = z.object({
  name: z.string().min(2),
  phone: z.string().regex(/^01[0-9]{8,9}$/, "올바른 휴대폰번호를 입력해주세요."),
});

export const requestPasswordResetSchema = z.object({
  email: z.string().email(),
});

export const verifyResetCodeSchema = z.object({
  email: z.string().email(),
  code: z.string().length(6),
});

export const resetPasswordSchema = z
  .object({
    token: z.string(),
    password: z.string().min(8).regex(/^(?=.*[a-zA-Z])(?=.*\d)/),
    confirmPassword: z.string(),
  })
  .refine((d) => d.password === d.confirmPassword, {
    message: "비밀번호가 일치하지 않습니다.",
    path: ["confirmPassword"],
  });
```

### 5.2 신규 Server Actions — `server/actions/find-account.actions.ts`

| 함수 | 역할 | 반환 |
|------|------|------|
| `findUserId(formData)` | 이름+전화번호로 계정 조회, 마스킹 이메일 반환 | `ApiResponse<{ maskedEmail: string } \| null>` |
| `requestPasswordReset(formData)` | 이메일로 인증코드 발송, `PasswordResetToken` 레코드 생성 | `ApiResponse` (계정 존재 무관 항상 success) |
| `verifyResetCode(formData)` | 코드 검증, 성공 시 1회용 `token` 발급 | `ApiResponse<{ token: string }>` |
| `resetPassword(formData)` | 토큰 검증 후 새 비밀번호 저장 + 기존 세션 무효화 | `ApiResponse` |

### 5.3 처리 흐름 의사코드

```ts
// requestPasswordReset
const user = await db.user.findUnique({ where: { email } });
// 존재 여부와 무관하게 동일 응답 (계정 열거 방지)
if (user) {
  await rateLimitCheck(`reset-code:${email}`); // 1분 1회
  const code = generate6DigitCode();
  await db.passwordResetToken.create({
    data: { userId: user.id, code: await hash(code, 10), token: randomToken(), expiresAt: addMinutes(now, 5) },
  });
  await sendPasswordResetCode(user.email, code);
}
return { success: true }; // 항상 동일 메시지: "인증코드를 발송했습니다"
```

```ts
// verifyResetCode
const record = await findLatestUnusedToken(email);
if (!record || record.expiresAt < now) return fail("코드가 만료되었습니다.");
if (record.attempts >= 5) return fail("시도 횟수를 초과했습니다. 다시 요청해주세요.");
const ok = await compare(code, record.code);
if (!ok) { await incrementAttempts(record.id); return fail("인증코드가 일치하지 않습니다."); }
await markVerified(record.id);
return { success: true, data: { token: record.token } };
```

```ts
// resetPassword
const record = await db.passwordResetToken.findUnique({ where: { token } });
if (!record || !record.verified || record.usedAt || record.expiresAt < now) return fail("유효하지 않은 요청입니다.");
await db.user.update({ where: { id: record.userId }, data: { password: await hash(newPassword, 12) } });
await db.passwordResetToken.update({ where: { id: record.id }, data: { usedAt: now } });
await db.session.deleteMany({ where: { userId: record.userId } }); // 전체 세션 무효화
return { success: true };
```

---

## 6. 화면(UI) 설계

### 6.1 라우팅 (`app/(auth)/` 그룹에 추가)

| 경로 | 설명 |
|------|------|
| `/find-id` | 이름+전화번호 입력 → 결과(마스킹 이메일) 표시 |
| `/find-password` | 이메일 입력 → 인증코드 입력 (2-step, 같은 페이지 내 단계 전환) |
| `/reset-password?token=xxx` | 새 비밀번호 설정 (코드 검증 성공 후 진입, 토큰 없으면 `/find-password`로 리다이렉트) |

### 6.2 로그인 페이지 연동

`app/(auth)/login/page.tsx` 하단에 링크 추가:

```tsx
<div className="mt-2 flex justify-center gap-3 text-sm text-[var(--color-text-secondary)]">
  <Link href="/find-id" className="hover:underline">아이디 찾기</Link>
  <span>|</span>
  <Link href="/find-password" className="hover:underline">비밀번호 찾기</Link>
</div>
```

### 6.3 컴포넌트 재사용

기존 `Card`, `CardHeader`, `Input`, `Label`, `Button` (components/ui/*) 그대로 재사용하여 `/login`, `/register`와 동일한 톤앤매너 유지. `react-hook-form` + `zodResolver` 패턴도 동일하게 적용.

### 6.4 `/find-password` 단계별 화면 상태 (클라이언트 state machine)

```
STEP_EMAIL (이메일 입력)
   └─ 제출 → requestPasswordReset() → STEP_CODE
STEP_CODE (인증코드 입력, 남은 시간 타이머 표시, "재발송" 버튼 60초 쿨다운)
   └─ 제출 → verifyResetCode() → STEP_DONE(token 저장) → router.push(`/reset-password?token=...`)
```

---

## 7. middleware.ts 반영

`/find-id`, `/find-password`, `/reset-password`는 **비로그인 사용자 전용**이 아니라(로그인 상태에서도 접근 허용 필요 없음) — 로그인/회원가입과 동일하게 **게스트 전용 라우트**로 분류할지 검토. 실무적으로는 로그인 상태에서도 접근을 막을 필요는 없으므로, `middleware.ts`의 게스트 전용 리스트에는 추가하지 않고 **공개 라우트**로 등록한다 (현재 로그인 여부와 무관하게 접근 가능).

---

## 8. 구현 단계 (Phase)

| Phase | 작업 | 산출물 |
|-------|------|--------|
| Phase 1 | Prisma 스키마 변경 (`User.phone`, `PasswordResetToken`) + 마이그레이션 | `prisma/schema.prisma` |
| Phase 2 | 이메일 발송 인프라 구축 (Resend 연동) | `lib/email.ts`, `.env.example` 갱신 |
| Phase 3 | Zod 스키마 + Server Actions 구현 | `lib/validations/find-account.schema.ts`, `server/actions/find-account.actions.ts` |
| Phase 4 | Rate Limit 추가 (`findAccountRateLimit`, 코드 재발송 제한) | `lib/rate-limit.ts` |
| Phase 5 | `/find-id`, `/find-password`, `/reset-password` 페이지 UI 구현 | `app/(auth)/find-id/page.tsx` 등 |
| Phase 6 | `/login` 페이지에 링크 연동 | `app/(auth)/login/page.tsx` |
| Phase 7 | 회원가입 폼에 `phone` 입력란 추가 (필수화 여부 결정 필요) | `app/(auth)/register/page.tsx`, `auth.schema.ts` |
| Phase 8 | 테스트 작성 (유효성 검증, 마스킹 로직, 코드 만료/시도횟수 로직) | `__tests__/lib/find-account.test.ts` |
| Phase 9 | 보안 점검 (계정 열거 방지 응답 일관성, rate limit 동작 확인) | QA 체크리스트 |

---

## 9. 테스트 계획

| 테스트 항목 | 검증 내용 |
|-------------|-----------|
| 이메일 마스킹 함수 | `pax@gmail.com` → `pa*@gmail.com` 등 로컬파트 길이별 엣지케이스 |
| 존재하지 않는 계정 조회 | 존재하는 계정과 **동일한 성공 응답** 반환 확인 |
| 인증코드 만료 | TTL 5분 경과 후 검증 실패 확인 |
| 인증코드 시도 횟수 제한 | 5회 오입력 후 코드 폐기 확인 |
| 재설정 토큰 재사용 방지 | 1회 사용 후 동일 토큰으로 재요청 시 실패 확인 |
| Rate Limit | 동일 IP/이메일 반복 요청 시 429 유사 응답(제한 메시지) 확인 |
| 비밀번호 재설정 후 세션 무효화 | 재설정 완료 후 기존 세션으로 API 호출 시 인증 실패 확인 |

---

## 10. 미해결 결정 사항 (구현 착수 전 확인 필요)

| 항목 | 옵션 | 권장 |
|------|------|------|
| 이메일 발송 서비스 | Resend / Nodemailer+SMTP / Supabase | **Resend** |
| 회원가입 시 `phone` 필수 여부 | 필수 / 선택 | **필수** (선택 시 기능 사각지대 발생) |
| 기존 회원 `phone` 백필 방법 | 관리자 일괄 등록 / 마이페이지 자진 등록 유도 | **마이페이지 자진 등록 유도 + 배너 안내** |
| 아이디 찾기 결과 표시 방식 | 화면에 마스킹 이메일 즉시 표시 / 이메일로만 안내 발송 | **화면 즉시 표시** (UX 단순, 이메일 발송 인프라 부하 감소) — 보안 요구 수준이 높아지면 후자로 전환 |

---

## 11. 예상 영향 범위 요약

```
prisma/schema.prisma                              (수정: User.phone, PasswordResetToken 추가)
lib/email.ts                                       (신규)
lib/rate-limit.ts                                  (수정: findAccountRateLimit 추가)
lib/validations/find-account.schema.ts             (신규)
server/actions/find-account.actions.ts             (신규)
app/(auth)/find-id/page.tsx                        (신규)
app/(auth)/find-password/page.tsx                  (신규)
app/(auth)/reset-password/page.tsx                 (신규)
app/(auth)/login/page.tsx                          (수정: 링크 추가)
app/(auth)/register/page.tsx                       (수정: phone 입력란 추가)
lib/validations/auth.schema.ts                     (수정: registerSchema에 phone 추가)
.env.example                                       (수정: RESEND_API_KEY 등 추가)
__tests__/lib/find-account.test.ts                 (신규)
```
